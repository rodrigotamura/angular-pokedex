import { RollPokeballDirective } from './roll-pokeball.directive';

describe('RollPokeballDirective', () => {
  it('should create an instance', () => {
    const directive = new RollPokeballDirective();
    expect(directive).toBeTruthy();
  });
});
